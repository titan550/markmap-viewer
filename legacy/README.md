# Legacy Single-File App

This folder preserves the original single-file implementation for reference.

- `legacy/index.html` is the baseline behavior target for the refactor.
- Open it directly in a browser for parity checks.
